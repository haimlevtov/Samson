.next
node_modules
package-lock.json
src/db/types.ts

# Vendored skill and agent definitions. Third-party text, not our source — and
# reformatting it would make every future upstream diff unreadable.
.claude
.agents

# A design handoff, copied as it arrived. Its tables are its author's, and a
# reformat would make it a document nobody handed over.
docs/design
